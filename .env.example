# Copy to .env and fill in real values. .env is gitignored — never commit it.
# Sandbox OAuth credentials for the Doc Detective token-endpoint tests
# (docs/tutorial/access-api-keys.mdx). Get them from the Prove Developer
# Portal, Credentials tab of a Sandbox project.

# US Sandbox credentials
PROVE_CLIENT_ID=your-sandbox-client-id
PROVE_CLIENT_SECRET=your-sandbox-client-secret

# EU Sandbox — the "International" credentials from the same Credentials tab
PROVE_EU_CLIENT_ID=your-international-client-id
PROVE_EU_CLIENT_SECRET=your-international-client-secret
